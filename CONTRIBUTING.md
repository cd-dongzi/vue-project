# 参与贡献

感谢你关注这个 Vue 实战项目。提交变更前，请先阅读本指南，确保改动范围清晰、可复现且便于审查。

## 本地开发

项目代码位于 `vue-toutiao/` 目录。请在该目录执行：

```bash
npm install
npm run dev
npm run build
```

`npm run dev` 用于本地开发，`npm run build` 用于生成生产构建。提交前请至少确认受影响页面可正常打开，并在适用时完成构建。

## 提交建议

- 一个 Pull Request 聚焦一个明确问题，避免混入无关格式化或生成文件。
- 说明改动原因、影响范围和已执行的验证命令。
- 修改接口调用、登录状态或路由权限时，说明兼容性和回退方式。
- 不要提交 `.DS_Store`、构建产物、依赖目录、账号信息、访问令牌或其他敏感数据。
- 新增依赖前请说明用途、维护状态和替代方案；避免为简单功能引入大型依赖。

## Issue 与审查

提交缺陷报告时，请提供复现步骤、期望结果、实际结果和运行环境。审查意见应围绕可维护性、兼容性和安全性展开。涉及安全问题时，请遵循 [安全策略](SECURITY.md)，不要在公开 Issue 中披露可利用细节。
