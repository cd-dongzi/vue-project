# Contributing

感谢你帮助改进 `vue-project`。本项目欢迎问题报告、文档改进、测试、兼容性修复和经过验证的依赖更新。

## 开始之前

- 搜索现有 Issue 和 Pull Request，避免重复工作。
- 较大的功能或架构调整请先创建 Issue 讨论范围。
- 安全漏洞不要通过公开 Issue 报告，请阅读 [SECURITY.md](SECURITY.md)。
- 项目是 Vue 2 教学示例；修改应尽量聚焦、可审查，并说明对旧版浏览器和移动端行为的影响。

## 本地开发

旧版构建链建议使用 Node.js 16 和 npm 8：

```bash
cd vue-toutiao
npm ci
npm run dev
```

提交前至少运行：

```bash
npm run build
```

## Pull Request 要求

1. 清楚描述问题、解决方案和不在本次修改范围内的内容。
2. 每个 PR 聚焦一个主题，避免混入无关格式化或依赖升级。
3. UI 修改请提供截图或录屏；行为修改请说明手工验证步骤。
4. 修改接口调用、登录状态或路由权限时，说明兼容性和回退方式。
5. 新增网络请求时，说明目标域名、传输的数据、凭证策略和失败行为。
6. 不要提交 API Key、Token、Cookie、真实账号、构建产物或 `node_modules`。
7. 新增或升级依赖时，说明用途、维护状态、替代方案、上游变更和构建验证结果。
8. AI 生成的代码、文本或图片必须明确披露，并由提交者检查许可证、事实准确性和安全性。

维护者会根据可维护性、安全性、教学价值和向后兼容性决定是否合并。
